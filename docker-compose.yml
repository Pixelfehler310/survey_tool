services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8010:8000"
    volumes:
      - ./backend:/app
      - ./surveys:/app/surveys
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./data/survey.db
      - CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:3010
      - SURVEYS_PATH=/app/surveys
      - ADMIN_DASHBOARD_URL=http://localhost:3010/admin
      - FRONTEND_URL=http://localhost:3010
    command: sh -c "python -m alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    ports:
      - "3010:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - ../civic:/civic
    environment:
      - VITE_API_URL=http://localhost:8010
      - VITE_BACKEND_HOST=backend
      - VITE_TURNSTILE_SITE_KEY=${TURNSTILE_SITE_KEY:-}
    depends_on:
      - backend
    command: sh -c "pnpm install && pnpm run dev -- --host 0.0.0.0 --port 3000"

  # PostgreSQL for production-like testing (optional)
  # db:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_USER: survey
  #     POSTGRES_PASSWORD: survey
  #     POSTGRES_DB: survey_engine
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

volumes:
  postgres_data:
